{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="valeurBtn text-white bg-gold focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mx-4 my-4 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none ">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
